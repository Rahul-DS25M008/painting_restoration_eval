# Experimental dependencies: legacy recipe, NOT an exact execution lock.
# Reviewed 2026-09-04; package specifications below are deliberately unchanged.
# All 36 saved notebook manifests record Python 3.12.6 and their actual package
# versions. Some recorded versions fall outside this recipe's ranges (including
# matplotlib/scipy); installing this list does not reproduce those runs exactly.
# Consult the target producer's manifests/run_manifest.json and the N36
# reproducibility appendix before choosing or rebuilding an environment.
#
# Original Python 3.11 recipe, in a separate environment (clean installation
# has NOT been revalidated by the documentation review):
#   py -3.11 -m venv .venv-experiments
#   .\.venv-experiments\Scripts\Activate.ps1
#   python -m pip install --upgrade pip setuptools wheel
#   python -m pip install -r requirements_experiments.txt
#
# Dashboard deployment instead uses Python 3.12 and root requirements.txt.
# Do not merge the environments: this recipe retains older experimental pins,
# including Pillow and IOPaint, while the dashboard has its own requirements.
# Model weights, input data, and compatible CUDA hardware/drivers are separate
# prerequisites. Installing dependencies does not execute or validate notebooks.

--extra-index-url https://download.pytorch.org/whl/cu121

# Core scientific stack
numpy>=1.26,<2.0
pandas>=2.2,<2.4
matplotlib>=3.9,<3.11
pillow==9.5.0
opencv-python>=4.10,<4.13
scikit-image>=0.24,<0.26
scipy>=1.14,<1.16
imageio>=2.35,<3
tifffile>=2024.8,<2027
tqdm>=4.66,<5
PyYAML>=6.0,<7

# PyTorch CUDA 12.1 stack
torch==2.5.1+cu121
torchvision==0.20.1+cu121
torchaudio==2.5.1+cu121

# Evaluation / ML models
lpips==0.1.4
diffusers==0.27.2
transformers
safetensors
accelerate

# Inpainting / LaMa
iopaint==1.6.0

# Reporting / dashboard
# Retained legacy experimental dependencies; deployment uses requirements.txt.
streamlit
plotly

# Notebook support
ipykernel
jupyter>=1.1,<2
jmespath
