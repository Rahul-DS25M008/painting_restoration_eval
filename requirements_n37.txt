# Notebook 37 adapter-only dependencies.
#
# Install into the existing experimental environment only after its recorded
# torch/CUDA stack has been verified. HINT and MAT source trees plus released
# checkpoints are external assets and are not installed or downloaded here.
# The official repositories were inspected at the revisions pinned in
# config/experiments/hint_mat_selection.yaml.

einops>=0.7,<0.9
click>=8.1,<9
requests>=2.31,<3
ninja>=1.11,<2
timm>=0.9,<2

